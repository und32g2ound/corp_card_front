import Vue from 'vue';
import Vuex from 'vuex';
import fireStore from '@/store/fireStore';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    fireStore,
  },
});
